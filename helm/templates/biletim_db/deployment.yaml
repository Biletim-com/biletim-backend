{{- if .Values.biletim_db.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.biletim_db.app }}
spec:
  replicas: {{ $.Values.biletim_db.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.biletim_db.app }}
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: {{ .Values.biletim_db.app }}
    spec:
      containers:
        - name: {{ .Values.biletim_db.app }}
          image: {{ .Values.biletim_db.image }}
          {{- if and $.Values.biletim_db.command }}
          command: 
          {{- range $.Values.biletim_db.command }}
           - {{ . }}
          {{- end }}
          {{- end }}
          volumeMounts:
          {{- if and $.Values.biletim_db.volumeMounts }}
          {{- range $key, $value := .Values.biletim_db.volumeMounts }}
            - name: "{{ $key }}"
              mountPath: "{{ $value }}"
          {{- end }}
          {{- end }}
          # envFrom:
          # - configMapRef:
          #     name: {{ .Values.biletim_db.configMapName }}
          env:
          {{- if .Values.biletim_db.env }}
          {{- range $key, $value := .Values.biletim_db.env }}
            - name: "{{ $key }}"
              value: "{{ $value }}"
          {{- end }}
          {{- end }}
          resources:
            {{- toYaml .Values.biletim_db.resources | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.biletim_db.port }}
              protocol: TCP
      {{- if and $.Values.global.imagePullSecrets }}
      imagePullSecrets:
      {{- range $.Values.global.imagePullSecrets }}
        - name: {{ . }}
      {{- end }}
      {{- end }}
      {{- if and $.Values.biletim_db.volumes }}
      volumes:
      {{- range $.Values.biletim_db.volumes }}
        - name: {{ .name }}
          persistentVolumeClaim:
            claimName: {{ .persistentVolumeClaim.claimName }}
      {{- end }}
      {{- end }}
      restartPolicy: Always
{{- end }}

