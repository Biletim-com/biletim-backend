{{- if .Values.biletim_db.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.biletim_db.app }}
spec:
  type: NodePort
  ports:
  - name: http
    port: {{ .Values.biletim_db.port }}
    targetPort: {{ .Values.biletim_db.port }}
    nodePort: 30400
  selector:
    app: {{ .Values.biletim_db.app }}
{{- end }}


