<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Response</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
            background-color: #f9f9f9; /* Soft background */
            text-align: center;
            color: #4a4a4a; /* Neutral base color */
        }
        .circle {
            width: 300px;
            height: 300px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2); /* Subtle shadow */
        }
        .circle.success {
            background-color: #28a745; /* Vibrant green */
            color: #ffffff;
        }
        .circle.error {
            background-color: #dc3545; /* Vibrant red */
            color: #ffffff;
        }
        .icon {
            font-size: 150px;
            line-height: 0;
            text-shadow: 0px 2px 4px rgba(0, 0, 0, 0.3); /* Subtle shadow for icon */
        }
        .message {
            font-size: 1.7rem;
            font-weight: bold;
            margin-top: 10px;
            text-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1); /* Subtle shadow for text depth */
        }
        .message.success {
            color: #3c763d; /* Subtle green for success */
        }
        .message.error {
            color: #a94442; /* Muted red for error */
        }
        @media (max-width: 600px) {
            .circle {
                width: 150px;
                height: 150px;
            }
            .icon {
                font-size: 75px;
            }
            .message {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    {{#if errorMessage}}
    <div class="circle error">
        <span class="icon">✗</span>
    </div>
    <p class="message error">{{errorMessage}}</p>
    {{else}}
    <div class="circle success">
        <span class="icon">✔</span>
    </div>
    <p class="message success">Successful Payment</p>
    {{/if}}
</body>
</html>
